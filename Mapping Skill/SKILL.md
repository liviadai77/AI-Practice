name: analyze-integration-changelog description: Use this skill when the user wants to collect official platform changelogs or release notes, normalize them into a structured change source of truth, and use an internal skill or internal capability source to determine whether related capabilities, interfaces, or records are already linked or used, then assess impact, uncertainty, and risk, and generate stakeholder-ready reports. Official changelogs are the only change-detection source; docs and help centers are for explanation only.

Analyze Integration Changelog
Trigger Mode
This skill supports one fixed execution trigger:

Enter preset execution mode only when the user's input is exactly 执行任务
Exact match means:
no leading characters
no trailing characters
no leading spaces
no trailing spaces
no extra punctuation
no prefix or suffix text
If the input is not an exact match for 执行任务, do not enter the preset execution mode.